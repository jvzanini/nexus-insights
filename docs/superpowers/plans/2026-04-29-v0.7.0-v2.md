# Nexus Insights v0.7.0 — Implementation Plan (v2)

> **Status:** v2 — após review #1 da v1. Próxima passa para v3 (final).

## Changelog v1 → v2

- **Paralelização**: Fase 1 (T1–T6) marcada como paralela; cada componente isolado pode rodar em subagent dedicado.
- **Dependências explícitas**: cada task lista pré-requisitos.
- **Tasks adicionais**:
  - T13b: documentar `MATRIX_IA_INBOX_ID` em `lib/constants/matrix-ia.ts` para evitar magic number.
  - T20b: testar refatoração de `mensagens-nao-respondidas-filters.tsx` com nova toolbar (regressão).
  - T34b: rodar `next lint` + `tsc --noEmit` ao final da Fase 9.
  - T38b: telemetria do auto-save flow (audit log `setting_updated` carrega `auto_save: true`).
- **TDD**: cada componente novo começa por test; cada server action começa por test do happy path + erros.
- **Subagent dispatch plan**:
  - 3 subagents em paralelo na Fase 1 (UI base): T1+T2; T3; T4+T5+T6.
  - 2 subagents em paralelo na Fase 8 (tours): T28+T29; T30+T31.
  - 1 subagent em paralelo na Fase 9: T32 (catalog).
  - Outras fases sequenciais.
- **Riscos detalhados** quando há dependência entre tasks (ex: T20 depende de T17/T19/T2/T1).
- **Gaps de testes encontrados**:
  - T9 sidebar isActive sem teste — adicionar.
  - T36 testLlmConnection sem teste por provider — adicionar com fetch mock.
  - T20 FiltersDrawer sem teste de focus trap — adicionar.

## Notas para review #2

- Conferir nomes de arquivos finais (consistência);
- Garantir que cada step mostra código completo (TDD);
- Adicionar comandos exatos (jest, next build, tsc, lint);
- Definir critérios de aceitação por fase.
- Fechar v3.
