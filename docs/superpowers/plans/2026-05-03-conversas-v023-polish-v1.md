# Plan v1: Conversas v0.23 Polish

> Status: v1 inicial — pente-fino #1 a seguir.

**Goal:** Aplicar 19 ajustes em `/relatorios/conversas` (3 bugs críticos + UI polish + highlight busca).

## Tasks

T1 — Bug crítico: search no reportFilters (page.tsx)
T2 — Bug crítico: single-day filter (datetime-core + tests)
T3 — Bug crítico: sorting anti-duplicação (sorting-dialog)
T4 — Layout: badge ↵ Enter inline + remove span hint quebrando layout
T5 — Highlight busca: HighlightedText helper + aplicar em conversas-table + drill-down
T6 — Calendar: defaultMonth=today + tamanho fontes -1
T7 — Toolbar tabela: remove duplicação Ordenação 3 + formato Mostrando X-Y de Z + paginação no topo
T8 — ConversasPagination: novo algoritmo + Popover reticência + Popover atual
T9 — FiltersDialog: sections fechadas + handleClearOnlyFilters + header dinâmico
T10 — Chips X adesivo + remove lixeirinhas separadas
T11 — Tour: bump v4 + step pagination-top
T12 — Bump versão + CHANGELOG + STATUS
T13 — Verification + push + deploy + smoke
