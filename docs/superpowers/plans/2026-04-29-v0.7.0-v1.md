# Nexus Insights v0.7.0 — Implementation Plan (v1)

> **Status:** v1 — esqueleto da decomposição. Sujeita a review #1 → v2.
> **Spec:** `docs/superpowers/specs/2026-04-29-v0.7.0-design-v3.md`

**Goal:** Polimento UX (rebranding, sidebar, filtros, tour, Matrix IA, tabela responsiva, Conversas pente fino, sidebar bug, Agente Nex 2.0).

**Architecture:** Mudanças cirúrgicas + 2 redesigns localizados (Filtros/Drawer e LlmConfigCard). Componentes base novos isolados. TDD onde há lógica testável; smoke visual para puro layout.

**Tech Stack:** Next.js 16, React 19, Tailwind v4, base-ui, framer-motion, Jest + jest-mock-extended.

---

## Phases

### Fase 0 — Setup
- T0: criar branch + worktree (já estamos em main; criar `release/v0.7.0`).

### Fase 1 — Componentes UI base
- T1: `<Sheet>` (Drawer lateral via Dialog).
- T2: `<CollapsibleSection>` wrapper.
- T3: `<MultiSelectCheckbox>` extraído com busca + Selecionar todos/visíveis.
- T4: `<SearchableSelect>` single-select com busca + endAdornment.
- T5: `<TierBadge>` ($, $$, $$$, FREE).
- T6: `<PageShell variant>` wrapper de largura.

### Fase 2 — Rebranding (R1)
- T7: Login texto.
- T8: Sidebar texto topo.

### Fase 3 — Sidebar (R2 + R10)
- T9: `isActive` longest-prefix-match (folhas vs grupos).
- T10: visual active raiz (pílula sólida) + submenu (dot violet).

### Fase 4 — Largura útil (R3)
- T11: Layout protegido remove `max-w-7xl`.
- T12: Aplicar `<PageShell>` em todas as `page.tsx`.

### Fase 5 — Sincronização Matrix IA (R8)
- T13: `getInboxesForUser` helper + testes.
- T14: Substituir call sites em todas as páginas de relatório.
- T15: `tabs-shell` ganha `excludeTabValues`; páginas filtram tab Matrix IA quando aplicável.
- T16: Copy do `matrix-ia-toggle-card`; auditoria de queries; Nex tools.

### Fase 6 — Filtros + Ordenação (R5 + R6)
- T17: `MultiSelectCheckbox` com busca + Selecionar todos/visíveis (já em T3, mas integração nas Páginas).
- T18: relabel "Equipe" → "Departamento".
- T19: `<AppliedFiltersChips>`.
- T20: `<FiltersToolbar>` + `<FiltersDrawer>` (refator de `AdvancedFilters`).
- T21: `<SortingDrawer>` + chip "Ordenação · N" no toolbar interno da `ConversasTable`.

### Fase 7 — Conversas pente fino (R4)
- T22: `buildColumns(accountId)` factory + render coluna "Ações".
- T23: `labels-chips` neutro + sem cap.
- T24: render atributos chave:valor + `defaultVisible: true`.
- T25: empty state com botão "Limpar filtros".
- T26: smoke "0 conversas" (subagent compara count com banco).

### Fase 8 — Tour (R7 + R9)
- T27: `tour-overlay` fix `popoverHeight` + responsivo.
- T28: `conversas-tour` 9 etapas + data-tour markers.
- T29: `dashboard-tour` expandido.
- T30: `mensagens-nao-respondidas-tour` expandido.
- T31: novos tours (performance, equipe, distribuicao, origem-ia).

### Fase 9 — Agente Nex 2.0 (R11)
- T32: `lib/llm/catalog.ts` (PROVIDER_CATALOG, CostTier).
- T33: `<TierBadge>` (já em T5, ok).
- T34: `<SearchableSelect>` (já em T4) integrado em `LlmConfigCard`.
- T35: server action `validateInput` aceita modelo livre.
- T36: `testLlmConnection` profundo por provider (errorKind, creditOk).
- T37: `LlmConfigCard` refactor (Outro + custom input + atalhos URL + auto-save).
- T38: testes do auto-save flow.

### Fase 10 — Verificação + finalização
- T39: Smoke tests manuais (subagent).
- T40: Build production.
- T41: CHANGELOG + README + design-system.
- T42: Commit + push.
- T43: Deploy (já automatizado via Actions) + verificar health endpoint.

---

## Notas para review #1

- Detalhar cada task com Files/Steps/Tests/Commit.
- Especificar paralelização possível (T1–T6 podem ir em paralelo entre subagents).
- Adicionar tasks faltantes (descobrir gaps de testes).
- Confirmar ordem de dependências (ex: T20 depende de T1, T2, T3, T19).
- Anotar quais tasks têm subagent dedicado.
