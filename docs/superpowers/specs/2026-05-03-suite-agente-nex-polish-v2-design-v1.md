# Spec — Suite Agente Nex · Polish (v0.24.0) — v1

**Status:** rascunho. Pente-fino #1 abaixo. Spec consolidada na v3 (mesmo arquivo sem suffix).

## Escopo
- A. Remover EmptyConsumoState
- B. Donut espessura + gap + tooltip near-mouse
- C. Bar tag style sem cor
- D. Linha total sutil + setinha hover + cotação real + Whisper
- E. Input bar layout estável
- F. AudioPlayer speed margem
- G. Doc/release v0.22→v0.24

## Pente-fino #1 (14 achados, ver v3 §9)

EmptyConsumoState linha exata · donut tooltip atual fixed (não near-mouse) · bar tick PROVIDER_LABELS já usado · linha total tem `(N)` · setinha hover precisa `group` · cotação cache 4h documentado · Whisper nota refinar · input bar hint REMOVE causa reflow · AudioPlayer falta min-w · provider Badge SVG inline melhor · ui-ux-pro-max obrigatória · testes podem quebrar · bump 0.22→0.24 (não 0.23) · run subagent-driven-development.
